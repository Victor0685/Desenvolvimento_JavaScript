const nome = 'luiz' // script
const nome2 = "Luiz" // script
const nome3 = `luiz` // script
const num1 = 10 // number
const num2 =  10.52 // number
let nomeAluno // undefined = valor idefinido// valor nulo (nao aponta pra local nenhum para a memoria) {nao vou utilizar o valor} (quando for undefiled ele tem que ser declarada por let, pois não tem como ter um const underfined {uma constante sem valor})
const sobrenomeAluno = null // Nulo -> nao aponta pra local nenhum para a memoria (o meu valor é NULO) {pode utilizar o const pois a sua variavel vai ter o valor nulo constantemente}

// null é diferente de undefined (null é utilizado para desconfigurar uma variavel {colocar o valor de uma variavel como NULO}

const aprovado = true // ou pode ser false (aprovado pose se escrever tambem = boolean) {é u, valor lógico}

console.log(typeof num1, num1)// para descobrir o tipo da variavel



